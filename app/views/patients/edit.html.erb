<% content_for :title, "Editing patient" %>

<div class="md:w-2/3 w-full">
  <h1 class="font-bold text-4xl">Editing patient</h1>

  <%= render "form", patient: @patient %>

  <%= link_to "Show this patient", @patient, class: "btn btn-info" %>
  <%= link_to "Back to patients", patients_path, class: "btn btn-outline" %>
</div>
