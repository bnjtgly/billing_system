<% content_for :title, "New medicine" %>

<div class="md:w-2/3 w-full">
  <h1 class="font-bold text-4xl">New medicine</h1>

  <%= render "form", medicine: @medicine %>

  <%= link_to "Back to medicines", medicines_path, class: "btn btn-outline" %>
</div>
