<% content_for :title, "Editing charge category" %>

<div class="md:w-2/3 w-full">
  <h1 class="font-bold text-4xl">Editing charge category</h1>

  <%= render "form", charge_category: @charge_category %>

  <%= link_to "Show this charge category", @charge_category, class: "btn btn-secondary" %>
  <%= link_to "Back to charge categories", charge_categories_path, class: "btn btn-outline" %>
</div>
